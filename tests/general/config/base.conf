# This will be included in the resolved configuration even if it's not explicitly included in 1.conf
# because we'll specify this file as the base configuration in the hocon_library

base-conf.strings.a = "a"
base-conf.strings.b = "b"
base-conf.overwrite = "Overwrite this"
base-conf.up-ref = ${test-conf.obj.obj2}
