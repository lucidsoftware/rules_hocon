# This will be included in the resolved configuration because we're explicitly including 2.conf in 1.conf

test-conf.strings.two.x = "x"
test-conf.strings.two.y = "y"
test-conf.strings.two.z = "z"

test-conf.overwrite = "overwritten!"
